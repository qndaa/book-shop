import React from "react";
import {useForm} from "react-hook-form";

const CustomerPanel = (props) => {

    const {register, handleSubmit, formState: {errors}} = useForm();

    return <div>CUSOTMER PANEL</div>
}

export default CustomerPanel;
